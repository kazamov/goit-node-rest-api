{
    "configurations": [
        {
            "command": "npm run dev",
            "name": "Start API",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
